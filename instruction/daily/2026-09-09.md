# 2026-09-09

## 今日记录

- 时间校准：系统时钟曾偏差 1 天（误作 09-08），今天经 NTP 校正为 09-09 CST（`timedatectl` 确认 System clock synchronized: yes）
- 昨天（09-08）日志保留在 `2026-09-08.md`，未迁移

## 任务记录

- 新任务：接扫码模块进行 Python 接收，识别内容处理后发布 ROS2 话题
  - 硬件：BF SCAN 扫码枪（lsusb `9901:0301`，当前以 HID 键盘模式挂载，无新增 /dev/tty*）
  - 决策：扫码枪切换到 USB 串口（VCP/CDC）模式，预期出现 `/dev/ttyACM0`；pyserial 按行读取 ASCII 条码
  - 环境：pyserial 3.5、rclpy 均可用
  - 计划：写 `fun/scanner.py`（rclpy 节点 + pyserial），扫码数据发布到 `/scanner/barcode`（std_msgs/String），终端打印并追加日志；串口只探测 `/dev/ttyACM*` 避免占用雷达 `/dev/ttyUSB0`
  - 入口：`python3 start.py scanner`
  - 待办：用户按手册扫描配置码切换模式后插拔验证新设备

## 任务记录（扫码模块接收）

- 扫码枪已切换成功：lsusb 从 `9901:0301 SCAN KEYBOARD`（HID/usbhid）变为 `9901:0303 SCAN CDC`，出现 `/dev/ttyACM0`（maj 166，cdc_acm，组 dialout）
- 扫码枪串口参数：**USB 串口 / GBK 编码 / 115200 波特率**；`fun/scanner.py` 已按此配置（`DEFAULT_BAUDRATE=115200`，`BARCODE_ENCODING="gbk"`）
- 代码：`fun/scanner.py` = rclpy 节点 + pyserial 按行读取；自动探测 `/dev/ttyACM*`（避开雷达 ttyUSB0）；打印 + 追加 `scans.log` + 发布 `/scanner/barcode`（String）
- 关键点：rclpy 依赖 ROS 环境，采用模块顶层 try/except 懒加载，缺失时提示 `source /opt/ros/jazzy/setup.bash && source ~/ros2_ws/install/setup.bash`
- 端到端验证通过：`setsid python3 start.py scanner` 常驻后台（PID 17328，日志 /tmp/scanner.log），`ros2 topic echo /scanner/barcode` 收到 `data: '162534'`，scans.log 同步记录
- 现象待确认：一次观察约 12 条相同码 `162534`（15:06:34~56），疑扫码枪"连读/重复发送"默认开启，需与用户确认是否一次性扫了多次

## 任务记录（串口端口识别修复）

- 问题：雷达 + 扫码枪同时插入时，启动雷达会搞错端口
  - 根因：`open_lidar.py`/`slam.py` 的 `find_lidar_port()` 将所有串口排序取第一个，字母序 `ttyACM0` < `ttyUSB0`，扫码枪先枚举时雷达被错连到扫码枪端口
- 方案：新增 `fun/serial_ports.py` 统一稳定识别
  - 优先按 `/dev/serial/by-id/` 稳定链接匹配（名称含厂商/产品/序列号，不依赖 tty 序号）
    - 雷达：`Silicon_Labs`/`CP210`（by-id 实际名 `usb-Silicon_Labs_CP2102N_...`），兜底 PID `ea60`
    - 扫码枪：`BF_SCAN`，兜底 VID `9901`
  - 无 by-id 时回退 sysfs `idVendor:idProduct` 过滤
  - 找不到时列出当前全部串口及其 VID:PID 协助排查
- 三个入口均改用：`open_lidar.py`/`slam.py` → `serial_ports.find_lidar_port()`；`scanner.py` → `serial_ports.find_scanner_port()`
- 验证：雷达+扫码枪同时插入 → 雷达 `/dev/ttyUSB0`、扫码枪 `/dev/ttyACM0` 均正确识别；scanner 节点端到端连对串口无异常
- 后台测试进程已清理（scanner 节点 + ros2 topic echo）

## 任务记录（OpenMV 通信 + 扫码查询服务）

- 外接 OpenMV N6（USB 枚举 `MicroPython Pyboard Virtual Comm Port in FS Mode`，VID:PID `37c5:1206`，by-id 名含 `MicroPython`）→ `/dev/ttyACM1`
- 串口识别扩展：`serial_ports.py` 新增 `find_openmv_port()`（by-id `MicroPython` + sysfs `37c5` 兜底）
- 阅读 `~/Desktop/camera_copy`：农业机器人视觉项目（OpenMV N6 MicroPython）
  - 协议：命令经 REPL（USB VCP）由 `input()` 读取、`print()` 回显；`send()` 的 `#<payload>$` 帧走 UART3 物理引脚（USB 读不到）
  - 命令：TASK1 / SNAPSHOT / TRACK / IRRIGATION（分派表 DISPATCH）
  - 回显标志：`TASK1_OK: <六位码>` / `TASK1_TIMEOUT: <码>` / SNAPSHOT_OK / TRACK_OK / IRRIGATION_DONE / UNKNOWN
- 决策：任务性功能用 **ROS2 Service**；区分"任务(Service) vs 事件流(Topic)"：
  - OpenMV 任务 → Service；扫码是持续事件流 → 保持 Topic + 另加查询服务
- 新建 srv 包 `~/ros2_ws/src/openmv_msgs`：`srv/Command.srv`（`string command --- string result, bool success`），已 colcon build
- 新建 `fun/camera.py`：节点 `openmv_node`
  - Service `openmv_node/camera/command`：收命令→发串口→读回显解析→响应 `result/success`
  - 结果发布 `/camera/result`（String）+ 打印 + 追加 `camera.log`
- `scanner.py` 增强：新增 `scanner/query` 服务（`std_srvs/srv/Trigger`）返回最近条码缓存；未扫过 → `success=false, message=NO_SCAN`；read_loop 增加 `rclpy.spin_once` 泵服务回调
- `start.py` 注册 `camera`（现 4 个功能：lidar / slam / scanner / camera）
- 端到端验证：
  - `ros2 service call /camera/command openmv_msgs/srv/Command "{command: 'TASK1'}"` → `TASK1_TIMEOUT: 000000`（无目标块，符合预期）；camera.log 已记录
  - `ros2 service call /scanner/query std_srvs/srv/Trigger` → `NO_SCAN`（未扫码）
  - 雷达 by-id 在变为 `ttyUSB1` 后仍识别正确（`/dev/ttyUSB1`）

## 任务记录（上位机桥接节点）

- 需求：树莓派与上位机经 USB 虚拟串口连接（波特率 115200）：
  - 10Hz 周期发送坐标（SLAM TF map->base_link 的 x,y,yaw）
  - 监听上位机命令 → 调用 ROS2 服务 → 回传结果
- 待定项（不阻塞框架）：帧头/帧尾/字段分隔格式未确定（占位：`#HEAD...TAIL$`、分隔逗号、行尾 \r\n）；上位机完整命令表待用户给出后扩充
- 新建 `fun/bridge.py`：节点 `host_bridge`
  - 10Hz 定时器：`lookup_transform("map","base_link")` → `#POS,x,y,yaw$`；SLAM 未运行时发 `#POS,no_tf$`
  - 读线程：解析上行帧 → `self.commands` 命令表 → 调 service → 回传 `#RES,<内容>$`
  - 命令表现含：`TASK1`→/camera/command、`QR`→/scanner/query；未知命令回 `UNKNOWN:<cmd>`
  - 多线程 executor（MultiThreadedExecutor）：服务调用阻塞不影响 10Hz 坐标
  - 端口须 `-p` 指定（上位机 USB 设备插入后可把其 VID/PID 加到 serial_ports 做自动识别）
- `start.py` 注册 `bridge`（现 5 个功能：lidar / slam / scanner / camera / bridge）
- 验证（pty 虚拟串口对 + 模拟上位机脚本）：坐标帧持续输出（no_tf）；`#TASK1$`→回传 `TASK1_TIMEOUT: 000000`；`#QR$`→回传 `NO_SCAN`；`#HELLO$`→`UNKNOWN:HELLO`
- 排障：rclpy Future 的 `result()` 不支持 `timeout_sec` 参数 → 改 `_wait_future` 轮询（done() + 超时）